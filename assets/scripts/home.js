const swiper = new Swiper('.swiper', {
    // Optional parameters
    centeredSlides: true,
    loop: true,
  
    // If we need pagination
    pagination: {
      el: '.choise__pagination',
      clickable: true
    },

  });